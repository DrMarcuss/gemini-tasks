// ==========================================
// 🏗️ ТИПЫ ДАННЫХ GEMINI TASKS
// ==========================================

// 1. СТАТУСЫ ЗАДАЧ
// Мы ограничиваем выбор только этими значениями.
// Нельзя написать "finished" или "done", только 'completed'.
export type TaskStatus = 'new' | 'in_progress' | 'completed' | 'cancelled';

// 2. ПРИОРИТЕТЫ
// 1 = Низкий, 2 = Средний, 3 = Высокий
export type TaskPriority = 1 | 2 | 3;

// 3. ГЛАВНАЯ СТРУКТУРА ЗАДАЧИ (Как в Базе Данных)
export interface Task {
  id: number;               // Уникальный номер (1, 2, 3...)
  title: string;            // Название задачи
  description?: string;     // Описание (знак ? значит "не обязательно")
  status: TaskStatus;       // Статус (см. выше)
  priority: TaskPriority;   // Приоритет (см. выше)
  is_completed: boolean;    // Галочка выполнения
  created_at: string;       // Дата создания (строка)
  user_id?: string;         // ID владельца (если есть авторизация)
}

// 4. ДАННЫЕ ДЛЯ СОЗДАНИЯ НОВОЙ ЗАДАЧИ
// Когда мы создаем задачу, у неё еще нет ID и даты создания.
// Omit<Task, ...> берет тип Task и удаляет из него лишние поля.
export interface CreateTaskInput {
  title: string;
  description?: string;
  priority?: TaskPriority; // Если не указан, будет 1
}

// ==========================================
// 🎨 КОНСТАНТЫ ДЛЯ ИНТЕРФЕЙСА
// ==========================================

// Словарь: Перевод статусов на русский
export const STATUS_LABELS: Record<TaskStatus, string> = {
  new: 'Новая',
  in_progress: 'В работе',
  completed: 'Готово',
  cancelled: 'Отменена'
};

// Словарь: Цвета для разных приоритетов (классы Tailwind)
export const PRIORITY_COLORS: Record<TaskPriority, string> = {
  1: 'text-green-400 border-green-400/30 bg-green-400/10',  // Низкий - Зеленый
  2: 'text-yellow-400 border-yellow-400/30 bg-yellow-400/10', // Средний - Желтый
  3: 'text-red-400 border-red-400/30 bg-red-400/10'        // Высокий - Красный
};

// Словарь: Иконки приоритетов
export const PRIORITY_ICONS: Record<TaskPriority, string> = {
  1: '🟢',
  2: '🟡',
  3: '🔥'
};
